package com.gdrt.binarySearch.template2;

public class Peak {
    // findPeakElement
    // as leftmost element is -infinity, the sequence is starting to increase: all we need to do is
    // to find the first element that decreases the sequence.
    // similarly as rightmost element is -infinity, the sequence is decreasing in the end, hence
    // we need to find the rightmost element that increases the sequence.
}
